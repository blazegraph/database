# Testing that local properties are loaded and that they are overriden
# by properties in ${user.home}/build.properties.


# Overriden by ${user.home}/build.properties

junit.framework.TestCase2.enabled=true

color=red
