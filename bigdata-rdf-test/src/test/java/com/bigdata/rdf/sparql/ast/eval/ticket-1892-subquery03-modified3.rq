# The query itself is the same as ticket-1892-subquery03-modified1.rq
# The difference is in ticket-1892-subquery03-modifie3.trig and 
# ticket-1892-subquery03-modified3.srx

prefix ex: <http://www.example.org/schema#>
prefix in: <http://www.example.org/instance#>

select ?x where {
graph ?h {
  {select ?x where {?x ?p ?g}}
  }
}
