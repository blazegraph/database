This configuration is suitable for running a bigdata federation on a single
machine without using the installer (this is mainly used for testing).

You MUST start jini separately for this configuration (the standalone
configuration does not know where jini was installed).
