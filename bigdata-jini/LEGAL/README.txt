The lib directory contains some bundled library dependencies.  The licenses for
bundled dependencies are found in this directory (LEGAL).

This module has a dependency on Jini 2.1 and Apache Zookeeper.

There is also a dependency on the bigdata module.

Other dependencies exist and are described in the bigdata module.
