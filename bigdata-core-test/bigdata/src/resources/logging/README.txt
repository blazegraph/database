The log4j.propeties and logging.properties files in this directory age staged
into the distribution area so they wind up in distributions and are also the
log files used when running the unit tests in CI.
